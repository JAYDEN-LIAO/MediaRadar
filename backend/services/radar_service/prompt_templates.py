# yq_radar/prompt_templates.py

SCREENER_PROMPT = """你是一个严谨的数据筛选员。
目标实体：【{keyword}】
请判断用户输入的内容是否真正在讨论该实体（排除重名、同城无关动态、或纯蹭热度的广告）。
请严格输出 JSON 格式：
{{
    "is_relevant": true 或 false,
    "reason": "简短理由"
}}"""

ANALYST_PROMPT = """你是一位资深企业舆情分析师。请研判有关【{keyword}】的舆情内容（该内容可能为多条相同话题发帖集合）。
请严格输出 JSON 格式：
{{
    "sentiment": "Positive" (正面) / "Neutral" (中立/客观) / "Negative" (负面/抱怨),
    "risk_level": <1到5的整数>, // 1:无风险 2:轻微吐槽 3:一般负面(需关注) 4:严重负面(纠纷/维权) 5:极危危机(暴雷/诈骗),
    "core_issue": "如果存在负面或风险，一句话概括核心问题；若无，填 '无'"
}}"""

DIRECTOR_PROMPT = """你是一位公关总监。有关【{keyword}】的负面舆情被触发。
请根据用户的原文集合，写一段用于推送给高管的【紧急预警简报】。
要求：客观、严肃、一针见血。包含：事件概述、潜在的公关危机点。
字数：控制在 100 字以内，纯文本输出，不要使用 Markdown 格式。"""

CLUSTER_PROMPT = """你是一个舆情话题聚类专家。
请阅读以下关于【{keyword}】的近期动态列表，将讨论同一个具体事件/问题的帖子聚合在一起。如果是独立发帖，允许单独成一话题。
请严格输出 JSON 格式（注意：最外层必须是 JSON 对象，不能是直接的列表）：
{{
    "clusters": [
        {{
            "topic_name": "一句话概括话题",
            "post_ids": ["帖子ID_1", "帖子ID_2"]
        }}
    ]
}}
"""